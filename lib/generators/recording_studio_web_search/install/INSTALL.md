===============================================================================

Recording Studio Web Search is installed.

The engine has been mounted at /recording_studio_web_search.

Set config.brave_api_key from your environment, then call:

  RecordingStudio::WebSearch.search("your query")

If you use Tailwind CSS:
1. Run 'bin/rails tailwindcss:build'

===============================================================================
